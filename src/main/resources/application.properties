spring.application.name=backend

#Conexión con base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/ventas
spring.datasource.username=postgres
spring.datasource.password=1115120175

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=mVDa5C/ydsJY0SUliP+AwFwK7ZOmlDZfAEyGc3I3HdKmc9c4ivoOv4CrHqSQi6WuI4FZW1mjJxTmsxJPjuPXeQ==
app.jwt.expiration-ms=900000
app.jwt.refresh-expiration-ms=604800000

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://192.168.1.66:5173

logging.level.org.springframework.security=INFO

# Ruta física donde se guardan las imágenes
app.upload.dir=uploads/productos

# Tamaño máximo de archivo
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Evita que Spring exponga el stack trace en respuestas de error
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Marcar la BD actual como "ya en V2" sin re-ejecutar nada spring.flyway.baseline-version=1 o 2 <----
spring.flyway.baseline-version=0 
